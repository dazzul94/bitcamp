<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="java100.app.dao.BoardDao">

    <resultMap type="board" id="boardMap">
        <id column="no" property="no"/>
        <result column="title" property="title"/>
        <result column="conts" property="content"/>
        <result column="regdt" property="regDate"/>
        <result column="vwcnt" property="viewCount"/>
    </resultMap>
    
	<select id="findAll" resultMap="boardMap" parameterType="map">
		select no,title,regdt,vwcnt from ex_board

		<if test="titles != null">
			<where>
				<foreach collection="titles" item="title">
					or title like '${word}%'
				</foreach>
			</where>
		</if>
		order by
		<choose>
			<when test="orderColumn == 'title' and align == 'desc'">
				title desc
			</when>
			<when test="orderColumn == 'title' and align == 'asc'">
				title asc
			</when>
			<when test="orderColumn == 'no' and align == 'desc'">
				no desc
			</when>
			<otherwise>
				no asc
			</otherwise>
		</choose>
		
		limit #{startIndex}, #{size}
	</select>

    <select id="countAll" resultType="int">
        select count(*) from ex_board
    </select>
    
	<insert id="insert" parameterType="board">
		insert into ex_board(title,conts,regdt)
                            values(#{title},#{content},now())
	</insert>

	<update id="update" parameterType="board">
		update ex_board set title=#{title}, conts=#{content} where no=#{no}
	</update>
	<update id="updateViewCount">
	   update ex_board set vwcnt=vwcnt+RAND() * 100 where no=#{no}
	</update>

	<delete id="delete" parameterType="int">
		delete from ex_board where no=#{no}
	</delete>

	<select id="findByNo" resultMap="boardMap" parameterType="int">
        select no,title,conts,regdt,vwcnt from ex_board where no=#{no}
	</select> 
</mapper>  









